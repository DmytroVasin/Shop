<% if current_page.last? %>
  <li class='disabled'>
    <%= link_to raw(t 'views.pagination.last'), '#', remote: remote %>
  </li>
<% else %>
  <li>
    <%= link_to raw(t 'views.pagination.last'), url, remote: remote %>
  </li>
<% end %>

