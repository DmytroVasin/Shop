<% if current_page.first? %>
  <li class='disabled'>
    <%= link_to raw(t 'views.pagination.first'), '#', remote: remote %>
  </li>
<% else %>
  <li>
    <%= link_to raw(t 'views.pagination.first'), url, remote: remote %>
  </li>
<% end %>

