<% genders.each do |key, value| %>
  <li>
    <label>
      <%= check_box_tag 'gender_type', key, set_value_by_params(key, params[:gender_params]) %>
      <%= value %>
    </label>
  </li>
<% end %>
