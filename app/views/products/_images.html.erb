<%= link_to image_tag(color.image.small, class: 'img-circle'), '#', data: { :'image' => color.image.middle, :'zoom-image'=> color.image.large, :'color'=> color.image.colours.try(:first).try(:name) } %>
