<blockquote>
  <h3>Расцветки:</h3>
</blockquote>
<% if @colours.any? %>
  <div id="paginator">
    <%= paginate @colours, window: 2, remote: true %>
  </div>

  <table class="table table-striped table-bordered brand_table">
    <tr>
      <th>Color name</th>
      <th>Russian - Color name</th>
      <th style='width: 50px'>Actions</th>
    </tr>
    <tbody id="colours_body">
      <%= render @colours %>
    </tbody>
  </table>
<% end %>
<%= link_to 'Add color', new_admin_colour_path, class: 'btn btn-primary custom_black_btn' %>
