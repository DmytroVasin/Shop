  <tr>
    <td><%= colour.name %></td>
    <td><%= colour.name_rus %></td>
    <td>
      <%= link_to 'Delete', admin_colour_path(colour.id), method: :delete, confirm: 'Are you sure?', class: 'btn btn-danger' %>
    </td>
  </tr>
