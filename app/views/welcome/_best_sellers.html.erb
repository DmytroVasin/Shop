<li>
	<%= link_to image_tag( show_list_image(best_sellers), class: 'best_seller_image'), product_path(best_sellers) %>
	<div class="title"><%= truncate(best_sellers.title, length: 15) %></div>
	<div class="price"><span class="small">цена: </span><%= number_to_currency(best_sellers.price, unit: 'грн.', format: '%n %u') %></div>
</li>
