<li>
	<%= link_to image_tag( middle_image(best_sellers), class: 'best_seller_image'), product_path(best_sellers) %>
	<div class="title"><%= truncate(best_sellers.title, length: 15) %></div>
	<div class="price"><span class="small">цена: </span>&nbsp;&#8372;<%= number_to_currency(best_sellers.price, unit: 'грн.', format: '%n') %></div>
</li>