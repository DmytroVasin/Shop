Был принят новый заказ:
  Дата:
    <%= @order.updated_at.localtime.strftime("%b %d '%Y, %H:%M") %>
  Ссылка:
    <%= admin_order_url(@order.id) %>
  Объщая сумма денег:
     <%= number_to_currency(@order.total_price, unit: 'грн.', format: '%n %u') %>

